$("#owl-example").owlCarousel({
	navigation : false, // Show next and prev buttons, make false to hide
	slideSpeed : 300, // miliseconds
	paginationSpeed : 400,
	singleItem: true, // false shows all images
	autoPlay: 5000, //to rotate automatically
});